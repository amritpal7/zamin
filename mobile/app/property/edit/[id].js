// Dedicated edit route — renders the same Post form, but as a normal pushed
// screen (no tab bar, no nested navigator). The form reads the [id] param as editId.
export { default } from "../../(tabs)/post";
